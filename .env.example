# Configurações da API de Autenticação
# Copie este arquivo para .env e configure suas chaves

# Chave secreta para assinar os tokens JWT (use uma chave forte e única)
SECRET_KEY=sua-chave-secreta-super-forte-aqui-minimo-32-caracteres

# Algoritmo usado para assinar os tokens
ALGORITHM=HS256

# Tempo de expiração dos tokens em minutos
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Chave da API para controlar quem pode gerar tokens
API_KEY=sua-api-key-super-secreta-para-gerar-tokens